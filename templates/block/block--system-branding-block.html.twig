{#
/**
 * @file
 * Bootstrap Barrio's theme implementation for a branding block.
 *
 * Each branding element variable (logo, name, slogan) is only available if
 * enabled in the block configuration.
 *
 * Available variables:
 * - site_logo: Logo for site as defined in Appearance or theme settings.
 * - site_name: Name for site as defined in Site information settings.
 * - site_slogan: Slogan for site as defined in Site information settings.
 */
#}
{% set attributes = attributes.addClass('site-branding') %}
<div class="d-flex flex-wrap align-content-center align-items-center">
{% if site_logo or site_name %}
  <a href="/" title="{{ 'Digital Collections Hamilton College Library'|t }}" target="_blank" class="navbar-brand">
    {% if site_logo %}
      <object data="{{ site_logo }}" alt="{{ 'Hamilton College Library'|t }}" class="icon_logo img-fluid align-self-center" /> </object>
    {% endif %}
    <span class="ml-2 d-md-inline">College Library{#  site_name #}</span>
  </a>
{% endif %}
{% if site_slogan %}
  <div class="d-inline-flex align-center h-100 site-name-slogan">
    {{ site_slogan }}
  </div>
{% endif %}
</div>
